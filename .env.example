OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# Required for Firebase Admin token verification in API routes.
# Paste a single-line JSON service account value, or provide application default credentials in your runtime.
FIREBASE_SERVICE_ACCOUNT_KEY=
